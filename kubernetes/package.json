{
  "name": "kubernetes",
  "display_name": "Kubernetes",
  "description": "Starts a single-node kubernetes cluster",
  "parameters": [
    {
      "name": "ADVERTISE_ADDRESSr", 
      "index": "ADVERTISE_ADDRESS", 
      "type": "string", 
      "required": true
    }
  ],
  "main": "kubernetes/run.sh",
  "dependencies": [
    "kubernetes/run.sh",
    "utils/cachengo.sh",
    "kubernetes/calico.yaml"
  ]
}
