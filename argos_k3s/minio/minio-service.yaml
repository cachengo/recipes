apiVersion: v1
kind: Service
metadata:
  name: minio-service
  namespace: minio
spec:
  type: NodePort
  sessionAffinity: None
  ports:
    - port: 9000
      protocol: TCP
      name: minio
    - port: 9001
      protocol: TCP
      name: minio-console
  selector:
    app: minio